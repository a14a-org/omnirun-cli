{
  "name": "@omnirun/cli",
  "version": "0.7.1",
  "description": "CLI for managing OmniRun sandboxes",
  "type": "module",
  "bin": {
    "omni": "./dist/cli.js",
    "omnirun": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "node --enable-source-maps src/cli.ts",
    "test": "npm run build && node tests/cli.test.mjs",
    "pack:smoke": "npm pack --dry-run",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "engines": {
    "node": ">=24.18.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@omnirun/sdk": "^0.6.0",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^24.13.3",
    "typescript": "^6.0.3"
  }
}
