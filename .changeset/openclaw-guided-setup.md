---
"@omnirun/cli": patch
---

feat: guided OpenClaw setup when no local state exists. Prompts for API keys with masked input, generates minimal config on the sandbox, and auto-generates a gateway auth token.
